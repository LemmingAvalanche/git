# Manual testing
./bin-wrappers/git my-format-patch --quiet \
    --header-cmd=test-cmd 'HEAD^{/★meta: base}'..@

# ... run it through git executable (what an idea!)
./git-format-patch --quiet --header-cmd=./test-cmd 'HEAD^{/★meta: base}'..@

# Test command for --header-cmd
#!/bin/sh

printf "version: $GIT_FP_HEADER_CMD_VERSION\n"
printf "sha1: $GIT_FP_HEADER_CMD_SHA1\n"
printf "count: $GIT_FP_HEADER_CMD_COUNT\n"

# END Test command
