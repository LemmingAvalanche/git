# ... run it through git executable (what an idea!)
./git-format-patch --output-directory=patches --no-cover-letter --header-cmd=./test-cmd 'HEAD^{/★meta: base}'..@

# Test command for --header-cmd
#!/bin/sh

printf "X-Version: $GIT_FP_HEADER_CMD_VERSION\n"
printf "X-Sha1: $GIT_FP_HEADER_CMD_SHA1\n"
printf "X-Count: $GIT_FP_HEADER_CMD_COUNT\n"

# END Test command

# Diff command :)
git diff 'HEAD^{/★meta: base}' -- ':(exclude)git-meta/**'
